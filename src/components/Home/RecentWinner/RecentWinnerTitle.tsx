import { medium } from "@/app/layout";

export default function RecentWinnerTitle() {
  return (
    <h3
      className={`${medium.className} text-2xl text-text-primary sm:text-3xl`}
    >
      최근 우승 작품
    </h3>
  );
}
